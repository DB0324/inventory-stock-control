# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed -- the brief requires
# connection strings, keys and passwords to live only in the environment.

# Generate with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=pbde_cc1$t5ra4(!3%*e+2m+t$t%j6_+y5wiwk%e8pi&$m+nw&

# Pooled -- the app uses this. Hostname contains "-pooler".
DATABASE_URL=postgresql://inventory_owner:REDACTED@ep-dry-frost-b36ouqnj-pooler.c-4.ap-southeast-1.aws.neon.tech/inventory

DATABASE_URL_DIRECT=postgresql://inventory_owner:REDACTED@ep-dry-frost-b36ouqnj.c-4.ap-southeast-1.aws.neon.tech/inventory

# Business day boundary for the dashboard's "today" and "this week" (goal 8).
TIME_ZONE=Asia/Kolkata

# 0 because Neon's pooled endpoint already pools connections.
CONN_MAX_AGE=0
PGSSLMODE=require

# --- Production only (config.settings.prod) ---
ALLOWED_HOSTS=your-api.onrender.com
FRONTEND_ORIGIN=https://your-app.vercel.app
